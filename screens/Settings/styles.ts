import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  themes: {
    padding: 16,
  },

  version: {
    width: '100%',
    position: 'absolute',
    bottom: 50,
  },

  versionText: {
    textAlign: 'center',
  },
});

export default styles;
