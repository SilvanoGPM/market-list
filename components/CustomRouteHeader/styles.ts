import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    height: 50,
    padding: 8,
    flexDirection: 'row',
  },

  button: {
    width: '20%',
  },

  title: {
    flex: 1,
  },
});

export default styles;
