import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    height: 60,
    padding: 8,
    alignItems: 'center',
    flexDirection: 'row',
  },

  button: {
    width: '20%',
  },

  title: {
    flex: 1,
  },
});

export default styles;
